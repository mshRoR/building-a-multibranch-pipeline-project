pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "webhook for development branch"'
                sh 'echo "generic webhook testing using pipeline project"'
            }
        }
    }
}
