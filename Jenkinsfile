pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "webhook testing"'
                sh 'echo "generic webhook testing using pipeline project"'
            }
        }
    }
}
