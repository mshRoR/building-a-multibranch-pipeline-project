pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "webhook testing from development"'
                sh 'echo "generic webhook testing using pipeline project"'
            }
        }
    }
}
